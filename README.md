# minigrep

Rust製のシンプルなコマンドライン検索ツール。指定したファイルからキーワードを検索します。

## 使い方

```sh
cargo run -- <検索文字列> <ファイルパス>
```

### 例

```sh
cargo run -- hello poem.txt
```

## 機能

- コマンドライン引数から検索文字列とファイルパスを受け取る
- 引数のパース処理を専用関数(`parse_config`)に分離
- 指定されたファイルの内容を読み込み表示する
